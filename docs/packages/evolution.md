<!-- Generated by scripts/gen_package_reference.py. Do not edit by hand -->

# evolution

**GNOME's integrated mail, calendar and groupware client**

- **Version recorded:** 3.56.2
- **Categories:** `emcomm`, `workstation`
- **Upstream:** <https://wiki.gnome.org/Apps/Evolution>

## What it does

Evolution: GNOME's mail client with deep calendar/contacts integration and Exchange support.

## Why you would want it

The natural pick on a GNOME desktop (Debian's default install often carries it already, which the detection respects), and the most 'groupware' of the options. In the packet/Winlink workflow, messages fetched by PAT land in a local mailbox; a mail client is how a human reads and writes them.

## Before it will work

Nothing radio-side. To wire it to Winlink, point it at PAT's mailbox or use PAT's own web UI alongside it -- the packet profile's documentation covers the workflow.

## How it installs

- apt: `evolution`

## Known problems

Heaviest of the four; pulls a slice of GNOME on non-GNOME desktops.

## Keeping it current

- probe: apt policy
- strategy: apt_upgrade

## Where to get help with the software itself

The GNOME project.

*Source: [`catalog/packages/evolution.yaml`](../../catalog/packages/evolution.yaml)*
